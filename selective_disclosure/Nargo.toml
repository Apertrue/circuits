[package]
name = "selective_disclosure"
type = "bin"
authors = ["Apertrue"]
compiler_version = ">=1.0.0"
description = "Selective disclosure: prove an image was verified on-chain without revealing private data"

[dependencies]
poseidon = { tag = "v0.1.1", git = "https://github.com/noir-lang/poseidon" }
