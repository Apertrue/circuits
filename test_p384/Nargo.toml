[package]
name = "test_p384"
type = "bin"
authors = ["Apertrue"]
compiler_version = ">=1.0.0"

[dependencies]
# Use noir_bigcurve as git dependency for testing (same version as vendored)
noir_bigcurve = { git = "https://github.com/noir-lang/noir_bigcurve", tag = "v0.12.0" }
# BigNum for trait access
bignum = { git = "https://github.com/noir-lang/noir-bignum", tag = "v0.8.2" }
